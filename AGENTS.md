# 工具箱开发约定

- 用户要求后续同类小应用统一在此仓库管理，部署到 `https://boomyao.github.io/`，公开使用与分享。
- 首页是应用目录；每个应用使用 `/apps/<slug>/` 独立地址。应用源码放在 `apps/<slug>/`，首页信息统一登记在根目录 `apps.json`。
- 保留现有应用能力。应用须有返回工具箱的入口，所有资源、Worker、WASM、图标和许可链接须适配应用子目录。
- 采用静态前端部署。需要后端的新能力应先说明与 GitHub Pages 的边界，不要把服务端密钥写进前端。
- 只发布应用代码与公开资源；不提交用户演奏视频、音频、精修工程、缓存或本机绝对路径。
- 不创建传统单元测试文件。不编写代码注释，除非用户明确要求。
- 使用 `npm run setup` 安装已登记应用的依赖，`npm run build` 构建，`npm run check` 校验产物。`npm run preview` 提供与 GitHub Pages 一致的子目录预览。
- Vite 应用必须设置 `base: '/apps/<slug>/'`，构建产物放在本应用的 `dist/`。无构建步骤的静态应用使用 `public/`。
- `main` 分支通过 GitHub Actions 自动构建并发布。用户明确要求发布时再推送；单独要求提交代码不等于要求推送或发布。
- 发布后确认 Actions 部署成功，并检查首页、应用入口及关键资源的公开访问。
- 各应用保留自己的开源许可和第三方声明。顺奏沿用 GPL-2.0，音频处理与音乐判断能力的限制须如实保留。
